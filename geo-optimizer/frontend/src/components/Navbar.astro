---
const version = `v${__ENGINE_VERSION__}`;

// Il menu è diviso per intento: "Tools" sono le cose che l'utente può fare adesso,
// "Learn" quelle che spiegano. Prima erano 3 voci in barra contro 13 in un unico
// dropdown "More" che mescolava tool, guide, un PDF e le pagine di brand, quindi
// le voci in fondo non venivano mai viste — comprese /best-geo-tools/ e /research/,
// che sono le due pagine con più impression dopo la home.
type NavLink = { label: string; href: string; desc: string };
type NavGroup = { id: string; label: string; links: NavLink[] };

const navGroups: NavGroup[] = [
  {
    id: "tools",
    label: "Tools",
    links: [
      { label: "Audit", href: "/", desc: "Free GEO audit of any URL" },
      { label: "Compare", href: "/compare/", desc: "Score your site against another" },
      { label: "Analyze competitors", href: "/analyze-competitors/", desc: "Benchmark several domains at once" },
      { label: "ChatGPT visibility", href: "/chatgpt-visibility-checker/", desc: "Check whether your site is ready for AI answers" },
      { label: "Perplexity citation", href: "/perplexity-citation-checker/", desc: "See whether AI answers cite your domain" },
      { label: "AI SEO audit", href: "/ai-seo-audit/", desc: "Free ChatGPT and Perplexity readiness audit" },
      { label: "AI citation checker", href: "/tools/ai-citation-checker/", desc: "Is your brand cited by AI? Free check" },
      { label: "llms.txt generator", href: "/tools/llms-txt-generator/", desc: "Free AI-ready llms.txt from your sitemap" },
    ],
  },
  {
    id: "learn",
    label: "Learn",
    links: [
      { label: "Guides", href: "/guides/", desc: "Practical AI visibility guides" },
      { label: "Best GEO tools", href: "/best-geo-tools/", desc: "How to choose a GEO tool" },
      { label: "Research", href: "/research/", desc: "Princeton KDD + AutoGEO ICLR" },
      { label: "Methodology", href: "/methodology/", desc: "How GEO scores are calculated" },
      { label: "State of GEO", href: "/state-of-geo/", desc: "Live benchmark of audited sites" },
      { label: "GEO Readiness Manual", href: "/geo-readiness-manual/", desc: "160-page technical PDF guide" },
      { label: "Roadmap", href: "/roadmap/", desc: "What's coming next" },
      { label: "Manifesto", href: "/manifesto/", desc: "Why we built GeoReady" },
    ],
  },
];

// Promosso fuori dai dropdown
const featuredLink = { label: "Pricing", href: "/pricing/" };
---

<nav id="floating-nav" data-theme="light" class="fixed inset-x-0 top-0 z-50 pt-3 pb-2 transition-all duration-300">
  <div class="max-w-7xl mx-auto px-4 sm:px-6">
    <div id="nav-pill" class="nav-pill h-14 pl-4 pr-2 sm:pl-5 sm:pr-3 flex items-center justify-between gap-3 rounded-full backdrop-blur-2xl backdrop-saturate-150 transition-all duration-300">

    <!-- Logo -->
    <a href="/" class="flex items-center gap-2.5 shrink-0">
      <svg width="28" height="28" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <rect width="48" height="48" rx="11" fill="#0D9488"/>
        <path d="M11.74 13.71 A16 16 0 1 1 11.74 34.29" stroke="white" stroke-width="2.8" stroke-linecap="round"/>
        <path d="M8 24 C12 14 18 34 24 24 C30 14 36 34 40 24" stroke="white" stroke-width="2.1" stroke-linecap="round" stroke-opacity="0.72"/>
      </svg>
      <span class="font-display font-bold text-base tracking-tight">
        <span class="logo-geo">Geo</span><span class="logo-ready">Ready</span>
      </span>
      <span class="version-badge hidden sm:inline font-mono text-[10px] px-1.5 py-0.5 rounded">{version}</span>
    </a>

    <!-- Desktop nav -->
    <div class="hidden md:flex items-center gap-0.5">
      <!-- Grouped dropdowns -->
      {navGroups.map((g) => (
        <div class="relative" data-nav-group>
          <button
            id={`${g.id}-menu-btn`}
            class="more-btn px-3 py-1.5 rounded-full text-[13px] font-medium transition-colors flex items-center gap-1"
            aria-expanded="false"
            aria-controls={`${g.id}-menu`}
          >
            {g.label}
            <svg data-nav-chevron width="10" height="6" viewBox="0 0 10 6" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" class="transition-transform duration-200">
              <path d="M1 1L5 5L9 1" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
          <div
            id={`${g.id}-menu`}
            class="more-menu absolute top-full left-0 mt-3 w-72 backdrop-blur-2xl backdrop-saturate-150 rounded-2xl opacity-0 invisible translate-y-1 transition-all duration-200 overflow-hidden p-1"
          >
            {g.links.map((l) => (
              <a
                href={l.href}
                data-nav-link
                class="more-item flex flex-col gap-0.5 px-3 py-2.5 rounded-xl text-sm transition-colors"
              >
                <span class="more-item-title font-medium">{l.label}</span>
                <span class="more-item-desc text-xs">{l.desc}</span>
              </a>
            ))}
          </div>
        </div>
      ))}

      <!-- Featured: Pricing pill -->
      <a
        href={featuredLink.href}
        data-nav-link
        class="nav-link nav-link-featured ml-1 px-3 py-1.5 rounded-full text-[13px] font-medium transition-colors flex items-center gap-1.5"
      >
        <span class="w-1.5 h-1.5 rounded-full bg-accent-teal animate-pulse"></span>
        <span class="relative z-10">{featuredLink.label}</span>
      </a>

      <!-- Divider -->
      <span class="nav-divider mx-2 h-5 w-px" aria-hidden="true"></span>

      <!-- Right cluster -->
      <div class="flex items-center gap-1.5">
        <a
          href="https://github.com/Auriti-Labs/geo-optimizer-skill"
          target="_blank"
          rel="noopener"
          aria-label="GitHub repository"
          class="nav-icon-btn w-8 h-8 flex items-center justify-center rounded-full transition-colors"
        >
          <svg width="15" height="15" viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
            <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/>
          </svg>
        </a>
        <a
          href="https://app.geoready.dev/signup"
          class="group px-3.5 py-1.5 rounded-full bg-accent-teal text-white text-[13px] font-semibold hover:bg-accent-teal-dark transition-all shadow-[0_4px_12px_-2px_rgba(13,148,136,0.45)] hover:shadow-[0_6px_16px_-2px_rgba(13,148,136,0.55)] hover:-translate-y-px flex items-center gap-1.5"
        >
          <span>Get started</span>
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="transition-transform duration-200 group-hover:translate-x-0.5" aria-hidden="true">
            <line x1="5" y1="12" x2="19" y2="12"/>
            <polyline points="12 5 19 12 12 19"/>
          </svg>
        </a>
      </div>
    </div>

    <!-- Mobile burger — Pulse-themed: arc + wave morphing into X -->
    <button
      id="mobile-menu-btn"
      class="md:hidden relative w-10 h-10 flex items-center justify-center rounded-lg text-text-primary hover:bg-bg-subtle transition-colors"
      aria-label="Toggle menu"
      aria-expanded="false"
      aria-controls="mobile-menu"
    >
      <span class="sr-only">Menu</span>
      <svg id="burger-icon" width="22" height="22" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" class="transition-transform duration-300">
        <!-- Arc (top) — morphs to X stroke 1 -->
        <path id="burger-arc" d="M5 8 A 7 7 0 0 1 19 8" stroke="currentColor" stroke-width="2" stroke-linecap="round" fill="none" class="transition-all duration-300"/>
        <!-- Wave (middle) — fades out -->
        <path id="burger-wave" d="M4 13 C 7 10 9 16 12 13 C 15 10 17 16 20 13" stroke="#0D9488" stroke-width="2" stroke-linecap="round" fill="none" class="transition-all duration-300"/>
        <!-- Bottom line — morphs to X stroke 2 -->
        <line id="burger-base" x1="6" y1="18" x2="18" y2="18" stroke="currentColor" stroke-width="2" stroke-linecap="round" class="transition-all duration-300"/>
      </svg>
    </button>
    </div>
  </div>

  <!-- end inner container -->
</nav>

<!-- Mobile fullscreen overlay — OUTSIDE nav to avoid backdrop-filter containing block bug -->
<div
  id="mobile-menu"
  class="md:hidden hidden fixed inset-x-0 top-[76px] bottom-0 z-[60] bg-bg-base overflow-y-auto overscroll-contain"
  role="navigation"
  aria-label="Mobile navigation"
  aria-hidden="true"
>
    <div class="px-5 pt-5 pb-10">

      <!-- Featured CTA -->
      <a
        href="/pricing/"
        data-mobile-link
        class="group flex items-center justify-between gap-3 rounded-xl border border-accent-teal/30 bg-accent-teal/10 px-4 py-3.5 mb-5 hover:border-accent-teal/60 transition-colors"
      >
        <div class="min-w-0">
          <div class="flex items-center gap-1.5 mb-0.5">
            <span class="w-1.5 h-1.5 rounded-full bg-accent-teal animate-pulse"></span>
            <span class="text-[9px] font-mono uppercase tracking-[0.15em] text-accent-teal">Featured</span>
          </div>
          <p class="font-display text-[15px] font-semibold text-text-primary leading-tight">Pricing — plans from $0</p>
          <p class="text-[11px] text-text-secondary mt-0.5 truncate">Free audit · Pro · Studio · Agency</p>
        </div>
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="shrink-0 text-accent-teal group-hover:translate-x-0.5 transition-transform" aria-hidden="true">
          <line x1="5" y1="12" x2="19" y2="12"/>
          <polyline points="12 5 19 12 12 19"/>
        </svg>
      </a>

      {navGroups.map((g) => (
        <div>
          <p class="text-[10px] font-mono uppercase tracking-[0.15em] text-text-muted mb-1.5 mt-5 px-1">{g.label}</p>
          <div class="flex flex-col mb-2">
            {g.links.map((l) => (
              <a
                href={l.href}
                data-mobile-link
                class="flex flex-col gap-0.5 py-3 px-1 border-b border-border last:border-0 hover:bg-bg-subtle/50 transition-colors"
              >
                <span class="text-[15px] font-medium text-text-primary">{l.label}</span>
                <span class="text-[11px] text-text-secondary">{l.desc}</span>
              </a>
            ))}
          </div>
        </div>
      ))}

      <!-- Bottom CTAs -->
      <div class="flex flex-col gap-2.5 mt-7 pt-5 border-t border-border">
        <a
          href="https://app.geoready.dev/signup"
          data-mobile-link
          class="w-full text-center py-3 rounded-lg bg-accent-teal text-white font-semibold text-sm hover:bg-accent-teal-dark transition-colors"
        >
          Get started — free
        </a>
        <a
          href="https://app.geoready.dev/login"
          data-mobile-link
          class="w-full text-center py-3 rounded-lg border border-border text-text-primary font-semibold text-sm hover:bg-bg-subtle transition-colors"
        >
          Log in
        </a>
        <a
          href="https://github.com/Auriti-Labs/geo-optimizer-skill"
          target="_blank"
          rel="noopener"
          class="flex items-center justify-center gap-2 py-2.5 text-xs text-text-secondary hover:text-text-primary transition-colors"
        >
          <svg width="14" height="14" viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/>
          </svg>
          View on GitHub
        </a>
      </div>
    </div>
</div>

<script>
  // ─── Mobile fullscreen menu ─────────────────────────────────────────────────
  const burgerBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const burgerIcon = document.getElementById('burger-icon');

  function setBurgerOpen(open: boolean) {
    const arc = document.getElementById('burger-arc') as SVGPathElement | null;
    const wave = document.getElementById('burger-wave') as SVGPathElement | null;
    const base = document.getElementById('burger-base') as SVGLineElement | null;
    if (!arc || !wave || !base) return;
    if (open) {
      // Morph arc → diagonal X line, wave fades, base → opposite diagonal
      arc.setAttribute('d', 'M6 6 L 18 18');
      wave.style.opacity = '0';
      base.setAttribute('x1', '18');
      base.setAttribute('y1', '6');
      base.setAttribute('x2', '6');
      base.setAttribute('y2', '18');
    } else {
      arc.setAttribute('d', 'M5 8 A 7 7 0 0 1 19 8');
      wave.style.opacity = '';
      base.setAttribute('x1', '6');
      base.setAttribute('y1', '18');
      base.setAttribute('x2', '18');
      base.setAttribute('y2', '18');
    }
  }

  function openMobile() {
    if (!mobileMenu) return;
    mobileMenu.classList.remove('hidden');
    mobileMenu.setAttribute('aria-hidden', 'false');
    burgerBtn?.setAttribute('aria-expanded', 'true');
    setBurgerOpen(true);
    document.body.style.overflow = 'hidden';
  }

  function closeMobile() {
    if (!mobileMenu) return;
    mobileMenu.classList.add('hidden');
    mobileMenu.setAttribute('aria-hidden', 'true');
    burgerBtn?.setAttribute('aria-expanded', 'false');
    setBurgerOpen(false);
    document.body.style.overflow = '';
  }

  burgerBtn?.addEventListener('click', () => {
    const isOpen = burgerBtn.getAttribute('aria-expanded') === 'true';
    isOpen ? closeMobile() : openMobile();
  });

  // Chiudi al tap su un link mobile
  document.querySelectorAll('[data-mobile-link]').forEach((link) => {
    link.addEventListener('click', closeMobile);
  });

  // Escape chiude
  document.addEventListener('keydown', (e: KeyboardEvent) => {
    if (e.key === 'Escape' && burgerBtn?.getAttribute('aria-expanded') === 'true') {
      closeMobile();
    }
  });

  // Chiudi se viewport cresce oltre md (768px)
  const mq = window.matchMedia('(min-width: 768px)');
  mq.addEventListener('change', (e) => {
    if (e.matches) closeMobile();
  });

  // ─── Desktop grouped dropdowns ───────────────────────────────────
  // The bar now has two menus ("Tools", "Learn"), so opening one has to close the
  // other, and moving focus out of one has to close it too — otherwise it stays
  // open over the page while focus is already somewhere else.
  const navGroupEls = Array.from(document.querySelectorAll<HTMLElement>('[data-nav-group]'));

  function setGroupOpen(group: HTMLElement, open: boolean) {
    const btn = group.querySelector<HTMLButtonElement>('button');
    const menu = group.querySelector<HTMLElement>('.more-menu');
    const chevron = group.querySelector<SVGElement>('[data-nav-chevron]');
    if (!btn || !menu) return;

    menu.classList.toggle('opacity-0', !open);
    menu.classList.toggle('invisible', !open);
    menu.classList.toggle('translate-y-1', !open);
    menu.classList.toggle('opacity-100', open);
    menu.classList.toggle('visible', open);
    menu.classList.toggle('translate-y-0', open);
    btn.setAttribute('aria-expanded', open ? 'true' : 'false');
    chevron?.style.setProperty('transform', open ? 'rotate(180deg)' : '');
  }

  function closeAllGroups(except?: HTMLElement) {
    navGroupEls.forEach((g) => {
      if (g !== except) setGroupOpen(g, false);
    });
  }

  navGroupEls.forEach((group) => {
    const btn = group.querySelector<HTMLButtonElement>('button');
    if (!btn) return;

    group.addEventListener('mouseenter', () => {
      closeAllGroups(group);
      setGroupOpen(group, true);
    });
    group.addEventListener('mouseleave', () => setGroupOpen(group, false));

    btn.addEventListener('keydown', (e: KeyboardEvent) => {
      if (e.key === 'Enter' || e.key === ' ') {
        e.preventDefault();
        const isOpen = btn.getAttribute('aria-expanded') === 'true';
        closeAllGroups(group);
        setGroupOpen(group, !isOpen);
      }
    });

    group.addEventListener('keydown', (e: KeyboardEvent) => {
      if (e.key === 'Escape') {
        setGroupOpen(group, false);
        btn.focus();
      }
    });

    group.addEventListener('focusout', (e: FocusEvent) => {
      if (!group.contains(e.relatedTarget as Node)) setGroupOpen(group, false);
    });
  });

  document.addEventListener('click', (e) => {
    const inside = navGroupEls.some((g) => g.contains(e.target as Node));
    if (!inside) closeAllGroups();
  });

  // ─── Floating pill scroll-shrink ─────────────────────────────────────────────
  const floatNav = document.getElementById('floating-nav');
  const navPill = document.getElementById('nav-pill');

  function updateNavScroll() {
    if (!floatNav || !navPill) return;
    const scrolled = window.scrollY > 8;
    if (scrolled) {
      floatNav.classList.remove('pt-3');
      floatNav.classList.add('pt-2');
      navPill.classList.remove('h-14');
      navPill.classList.add('h-12');
      navPill.dataset.scrolled = 'true';
    } else {
      floatNav.classList.add('pt-3');
      floatNav.classList.remove('pt-2');
      navPill.classList.add('h-14');
      navPill.classList.remove('h-12');
      navPill.dataset.scrolled = 'false';
    }
  }

  updateNavScroll();
  window.addEventListener('scroll', updateNavScroll, { passive: true });

  // ─── Auto contrast via IntersectionObserver ─────────────────────────────────
  // Sections with class .nav-dark or [data-nav-theme="dark"] flip nav to dark theme
  // when they cross under the navbar. Detection point: top of viewport + nav height.
  const darkSections = document.querySelectorAll<HTMLElement>(
    '.nav-dark, [data-nav-theme="dark"], .bg-bg-dark, [class*="bg-bg-dark"]'
  );

  if (darkSections.length && floatNav) {
    const NAV_OFFSET = 80; // approx nav height + padding
    const observer = new IntersectionObserver(
      (entries) => {
        // Find any dark section currently intersecting the nav line
        let inDark = false;
        document.querySelectorAll<HTMLElement>(
          '.nav-dark, [data-nav-theme="dark"], .bg-bg-dark, [class*="bg-bg-dark"]'
        ).forEach((sec) => {
          const rect = sec.getBoundingClientRect();
          if (rect.top <= NAV_OFFSET && rect.bottom >= NAV_OFFSET) {
            inDark = true;
          }
        });
        floatNav.dataset.theme = inDark ? 'dark' : 'light';
      },
      { rootMargin: `-${NAV_OFFSET}px 0px 0px 0px`, threshold: [0, 1] }
    );
    darkSections.forEach((s) => observer.observe(s));

    // Also check on scroll (covers sections taller than viewport)
    window.addEventListener('scroll', () => {
      let inDark = false;
      darkSections.forEach((sec) => {
        const rect = sec.getBoundingClientRect();
        if (rect.top <= NAV_OFFSET && rect.bottom >= NAV_OFFSET) inDark = true;
      });
      floatNav.dataset.theme = inDark ? 'dark' : 'light';
    }, { passive: true });
  }

  // ─── Active link highlight (current page pill) ───────────────────────────────
  const navLinks = document.querySelectorAll<HTMLAnchorElement>('[data-nav-link]');
  const currentPath = window.location.pathname.replace(/\/$/, '') || '/';
  navLinks.forEach((link) => {
    const href = link.getAttribute('href')?.replace(/\/$/, '') || '/';
    if (href === currentPath) {
      link.classList.add('nav-link-active');
      link.setAttribute('aria-current', 'page');
    }
  });

  // The current page usually sits inside a dropdown now, so mark its group button
  // too — otherwise nothing in the visible bar reflects where the user is.
  navGroupEls.forEach((group) => {
    if (group.querySelector('.nav-link-active')) {
      group.querySelector('button')?.classList.add('nav-link-active');
    }
  });
</script>

<style>
  /* Version badge — readable on glass, both themes */
  .version-badge {
    background: color-mix(in srgb, white 35%, transparent);
    border: 1px solid color-mix(in srgb, white 45%, transparent);
    color: rgba(20, 20, 20, 0.78);
    backdrop-filter: blur(4px);
  }
  #floating-nav[data-theme="dark"] .version-badge {
    background: color-mix(in srgb, white 14%, transparent);
    border-color: color-mix(in srgb, white 22%, transparent);
    color: rgba(255, 255, 255, 0.85);
  }

  /* Glassmorphism pill — true translucency, no opaque fill */
  .nav-pill {
    background: color-mix(in srgb, white 18%, transparent);
    border: 1px solid color-mix(in srgb, white 35%, transparent);
    box-shadow:
      0 1px 0 0 color-mix(in srgb, white 50%, transparent) inset,
      0 4px 24px -8px rgba(0, 0, 0, 0.12),
      0 2px 6px -2px rgba(0, 0, 0, 0.06);
  }
  .nav-pill[data-scrolled="true"] {
    background: color-mix(in srgb, white 28%, transparent);
    box-shadow:
      0 1px 0 0 color-mix(in srgb, white 60%, transparent) inset,
      0 10px 32px -10px rgba(0, 0, 0, 0.22),
      0 4px 12px -4px rgba(0, 0, 0, 0.10);
  }

  /* Dark theme — nav over dark sections (auto-flipped by JS) */
  #floating-nav[data-theme="dark"] .nav-pill {
    background: color-mix(in srgb, black 28%, transparent);
    border-color: color-mix(in srgb, white 18%, transparent);
    box-shadow:
      0 1px 0 0 color-mix(in srgb, white 12%, transparent) inset,
      0 6px 28px -10px rgba(0, 0, 0, 0.45);
  }
  #floating-nav[data-theme="dark"] .nav-pill[data-scrolled="true"] {
    background: color-mix(in srgb, black 42%, transparent);
  }

  /* ─── Primary nav links ──────────────────────────────────────────────────── */
  #floating-nav[data-theme="light"] .nav-link { color: rgba(20, 20, 20, 0.68); }
  #floating-nav[data-theme="light"] .nav-link:hover { color: rgba(20, 20, 20, 0.95); background: rgba(0, 0, 0, 0.05); }
  #floating-nav[data-theme="dark"] .nav-link { color: rgba(255, 255, 255, 0.78); }
  #floating-nav[data-theme="dark"] .nav-link:hover { color: white; background: rgba(255, 255, 255, 0.08); }

  /* Active page link */
  #floating-nav[data-theme="light"] .nav-link-active { background: rgba(0, 0, 0, 0.06); color: rgba(20, 20, 20, 0.95); }
  #floating-nav[data-theme="dark"] .nav-link-active { background: rgba(255, 255, 255, 0.10); color: white; }

  /* Featured (Pricing) — accent teal both themes */
  #floating-nav[data-theme="light"] .nav-link-featured { color: #115E59; }
  #floating-nav[data-theme="light"] .nav-link-featured:hover { background: rgba(17, 94, 89, 0.12); }
  #floating-nav[data-theme="dark"] .nav-link-featured { color: #5EEAD4; }
  #floating-nav[data-theme="dark"] .nav-link-featured:hover { background: rgba(94, 234, 212, 0.12); }

  /* More button (same as nav-link styling) */
  #floating-nav[data-theme="light"] .more-btn { color: rgba(20, 20, 20, 0.68); }
  #floating-nav[data-theme="light"] .more-btn:hover { color: rgba(20, 20, 20, 0.95); background: rgba(0, 0, 0, 0.05); }
  #floating-nav[data-theme="dark"] .more-btn { color: rgba(255, 255, 255, 0.78); }
  #floating-nav[data-theme="dark"] .more-btn:hover { color: white; background: rgba(255, 255, 255, 0.08); }

  /* Divider */
  #floating-nav[data-theme="light"] .nav-divider { background: rgba(0, 0, 0, 0.12); }
  #floating-nav[data-theme="dark"] .nav-divider { background: rgba(255, 255, 255, 0.18); }

  /* Icon button (GitHub) */
  #floating-nav[data-theme="light"] .nav-icon-btn { color: rgba(20, 20, 20, 0.65); }
  #floating-nav[data-theme="light"] .nav-icon-btn:hover { color: rgba(20, 20, 20, 0.95); background: rgba(0, 0, 0, 0.05); }
  #floating-nav[data-theme="dark"] .nav-icon-btn { color: rgba(255, 255, 255, 0.75); }
  #floating-nav[data-theme="dark"] .nav-icon-btn:hover { color: white; background: rgba(255, 255, 255, 0.08); }

  /* ─── More dropdown menu ─────────────────────────────────────────────────── */
  #floating-nav[data-theme="light"] .more-menu {
    background: color-mix(in srgb, white 78%, transparent);
    border: 1px solid rgba(0, 0, 0, 0.08);
    box-shadow: 0 12px 40px -8px rgba(0, 0, 0, 0.18), 0 4px 12px -4px rgba(0, 0, 0, 0.08);
  }
  #floating-nav[data-theme="dark"] .more-menu {
    background: color-mix(in srgb, #0a0a0a 72%, transparent);
    border: 1px solid rgba(255, 255, 255, 0.12);
    box-shadow: 0 12px 40px -8px rgba(0, 0, 0, 0.45), 0 4px 12px -4px rgba(0, 0, 0, 0.35);
  }

  /* Dropdown items */
  #floating-nav[data-theme="light"] .more-item-title { color: rgba(20, 20, 20, 0.95); }
  #floating-nav[data-theme="light"] .more-item-desc { color: rgba(20, 20, 20, 0.55); }
  #floating-nav[data-theme="light"] .more-item:hover { background: rgba(0, 0, 0, 0.05); }
  #floating-nav[data-theme="dark"] .more-item-title { color: white; }
  #floating-nav[data-theme="dark"] .more-item-desc { color: rgba(255, 255, 255, 0.62); }
  #floating-nav[data-theme="dark"] .more-item:hover { background: rgba(255, 255, 255, 0.08); }

  /* More chevron color follows theme via currentColor on the parent button — already handled */

  /* ─── Logo wordmark ──────────────────────────────────────────────────────── */
  #floating-nav[data-theme="light"] .logo-geo { color: #115E59; }
  #floating-nav[data-theme="light"] .logo-ready { color: rgba(20, 20, 20, 0.95); }
  #floating-nav[data-theme="dark"] .logo-geo { color: #5EEAD4; }
  #floating-nav[data-theme="dark"] .logo-ready { color: white; }
</style>
